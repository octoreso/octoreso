%dl.dl-horizontal
  %dt ID
  %dd= "##{@community.id}"
  %dt Name
  %dd= @community.name

%table.table.table-condensed.table-striped
  %thead
    %tr
      %th{ colspan: 3 } Missions
    %tr
      %th Mission Series
      %th Mission Name
      %th Link
  %tbody
    - @community.missions.each do |mission|
      %tr
        %td= mission.mission_series.try(:name) || ''
        %td
          %a{ href: mission.name }
            = mission.name
        %td= mission.mission_url
