!!!
%html{lang: 'en'}
  %head
    %meta{charset: 'utf-8'}/
    %meta{content: 'IE=edge', 'http-equiv' => 'X-UA-Compatible'}/
    %meta{content: 'width=device-width, initial-scale=1', name: 'viewport'}/
    %meta{content: '', name: 'description'}/
    %meta{content: '', name: 'author'}/
    %link{href: 'favicon.ico', rel: 'shortcut icon'}/
    %title Tobypinder.com
    = stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags
  %body
    .navbar.navbar-inverse.navbar-fixed-top{role: 'navigation'}
      .container
        .navbar-header
          %button.navbar-toggle{'data-target' => '.navbar-collapse', 'data-toggle' => 'collapse', type: 'button'}
            %span.sr-only Toggle navigation
            %span.icon-bar
            %span.icon-bar
            %span.icon-bar
          %a.navbar-brand{href: url_for(root_path)} TobyPinder.com
        .navbar-collapse.collapse 
          %ul.nav.navbar-nav
            %li 
              = link_to 'Music Monday', category_path(1)
          - if user_signed_in?
            .navbar-right.navbar-form

              = "Hello #{current_user.email}"
              = link_to 'Administrate', rails_admin_path if can? :access, :rails_admin
              = link_to 'Logout', destroy_user_session_path, method: :delete
          - else
            = simple_form_for(:user, url: user_session_path, html: {class: 'navbar-form navbar-right' }) do |f|
              .form-group
                = f.input_field :email, autofocus: true, class:'form-control', placeholder: 'Email'
              .form-group
                = f.input_field :password, label: false, class:'form-control', placeholder: 'Password'
              = f.button :submit, "Sign in", class: 'btn-success'
        / /.navbar-collapse
    %br/
    = yield
    %hr/
    %footer
    .container
      .row
        .col-md-12
          %p © Toby Pinder 2014. Contruction.gif goes here.
