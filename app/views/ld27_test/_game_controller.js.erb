var GameController = {
  init:function(token)
  {
    GameModel.init(token);
    GameView.init();
    GameModel.gameTimer = setInterval(GameController.loop,(1000/GameModel.stats.MAX_FPS));
    console.log("GameAJAX init")
    setInterval(function(){GameAjax.send();}, 1000);
  },
  loop:function()
  {
    
    GameModel.frame();
    GameView.frame();
    GameModel.benchmarkEnd();
    GameModel.benchmarkStart();
  }
}

